version: '3.8'

services:
  transcriber:
    build: .
    ports:
      - "5000:5000"
    devices:
      - /dev/snd:/dev/snd
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./app_data:/app/data
    restart: unless-stopped